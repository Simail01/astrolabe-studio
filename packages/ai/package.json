{
  "name": "@astrolabe/ai",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc && node -e \"require('fs').cpSync('src/prompts', 'dist/prompts', {recursive: true})\"",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@astrolabe/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^1.6.0"
  }
}
